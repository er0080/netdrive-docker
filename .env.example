# mTCP NetDrive Server Configuration
# Copy this file to .env and customize as needed

# Port the NetDrive server listens on (UDP)
NETDRIVE_PORT=2002

# Enable verbose logging (true/false)
NETDRIVE_VERBOSE=false

# Directory inside container where disk images are stored
# (Mount your local images directory to this path)
NETDRIVE_IMAGES_DIR=/data/images

# Network interface to bind to
# Use 0.0.0.0 to listen on all interfaces
# Use 127.0.0.1 for local access only
NETDRIVE_BIND_ADDRESS=0.0.0.0
